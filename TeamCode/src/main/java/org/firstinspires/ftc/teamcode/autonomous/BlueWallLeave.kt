package org.firstinspires.ftc.teamcode.autonomous

import com.acmerobotics.roadrunner.Pose2d
import com.acmerobotics.roadrunner.Vector2d
import com.acmerobotics.roadrunner.ftc.runBlocking
import com.qualcomm.robotcore.eventloop.opmode.Autonomous
import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode
import org.firstinspires.ftc.teamcode.roadrunner.PinpointDrive
/*      If you have an error I can't download to the robot!!!
class BlueWallLeave {

    package org.firstinspires.ftc.teamcode.autonomous.park



    @Autonomous
    class BlueAutoLeft : LinearOpMode() {
        override fun runOpMode() {
            val beginPose = Pose2d(60.0, 1.0, -Math.PI / 2) // was x = 30.5, y = 66
            val drive = PinpointDrive(hardwareMap, beginPose)
            waitForStart()
            runBlocking(
                drive.actionBuilder(beginPose)
                    .strafeTo(Vector2d(-0.0 , 0.0)) // was x = -41.5, y = 64.0
                    .build()
            )
        }
    }
}


 */